# Game server
PORT=8080
RAILS_INTERNAL_URL=http://localhost:8000

# Rails バックエンドとの共有シークレット
# backend/.env の同名項目にも、必ず同じ値を設定してください。
# openssl rand -hex 64
JWT_SECRET_KEY=your_shared_jwt_secret_key_here
INTERNAL_API_SECRET=your_shared_internal_api_secret_here
