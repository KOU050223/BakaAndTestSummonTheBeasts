pre-commit:
  parallel: true
  commands:
    backend-lint:
      root: backend
      run: nix develop "$(git rev-parse --show-toplevel)" --command bin/rubocop -f github
      env:
        RUBOCOP_CACHE_ROOT: tmp/rubocop

    frontend-lint:
      root: frontend
      run: pnpm lint

pre-push:
  commands:
    backend-test:
      root: backend
      run: nix develop "$(git rev-parse --show-toplevel)" --command bundle exec rspec
      env:
        RAILS_ENV: test

    backend-scan:
      root: backend
      run: nix develop "$(git rev-parse --show-toplevel)" --command sh -c "bin/brakeman --no-pager && bin/bundler-audit"
