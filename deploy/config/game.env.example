# host ネットワークで動かすため、外部公開を避けてループバックに bind する。
HOST=127.0.0.1
PORT=8080
# Rails Internal API の URL。同じ Pi 上の baka-backend を指す。
RAILS_INTERNAL_URL=http://127.0.0.1:8000
# Rails バックエンドと共有するシークレット。backend.env と同じ値を設定する。
JWT_SECRET_KEY=replace-me
INTERNAL_API_SECRET=replace-me
