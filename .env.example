# PostgreSQL (docker-compose)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=baka_user
DB_PASSWORD=your_password_here

# JWT署名に使用するRailsのシークレットキー（`bin/rails secret` で生成）
SECRET_KEY_BASE=your_secret_key_base_here
